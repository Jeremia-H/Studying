public class Kata
{
    public static bool IsSquare(int n)
    {
        if (n >= 0)
        {
            if (Math.Sqrt(n) % 1 == 0)
                return true;
        }
        return false;

        //Your code goes here!
    }
}
